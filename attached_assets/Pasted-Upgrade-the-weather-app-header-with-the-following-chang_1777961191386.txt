Upgrade the weather app header with the following changes. All changes must work together as a single cohesive system:

1. DYNAMIC HEADER BACKGROUND
The header background must change based on real weather condition from the API. Use a CSS gradient sky:
- Clear day → bright blue gradient (#4FC3F7 to #0277BD)
- Cloudy/Overcast → grey-blue gradient (#78909C to #546E7A)  
- Monsoon/Rain/Thunderstorm → deep blue-grey gradient (#37474F to #263238)
- Night → dark navy (#1A237E to #0D0D1A)
Do NOT use a static image. Build the sky with CSS gradients so it always adapts.

2. ANIMATED CELESTIAL BODY (SUN / MOON)
Render the sun and moon as animated SVG or CSS elements, NOT emoji or image assets. Rules:
- Sun: a golden circle with 8 animated rotating rays using CSS keyframes. Color: #FDD835 core, #FF8F00 glow. Size: 64px circle.
- Moon: a crescent SVG for night conditions.
- Position the sun/moon FIXED in the upper-right area of the header (top: 24px, right: 40px). It must NOT scroll with the page — use position: sticky or keep it inside a fixed-height header container.
- During overcast/rain, reduce sun opacity to 0.3 and show it behind cloud layers.

3. REALISTIC CLOUD LAYER IN HEADER ONLY
Add animated cloud shapes (pure CSS, no images) that float horizontally across the header ONLY — not the full page. Render 2–3 cloud divs using border-radius pill shapes stacked with ::before and ::after pseudo-elements. Rules:
- Clouds only appear when condition is: Cloudy, Overcast, Partly Cloudy, Mist, Rain, Thunderstorm, or Monsoon.
- Cloud animation: slow horizontal drift, 18s linear infinite loop, looping from right to left.
- Monsoon/Rain conditions: add a subtle vertical CSS rain animation (thin 1px lines falling) inside the header background.
- Cloud opacity: 0.85 for heavy clouds, 0.5 for partly cloudy.

4. HEADER LAYOUT FIX
Fix the header so the sun/celestial element is never clipped or hidden behind other UI elements. The header must have a minimum height of 180px. Place weather icon, city name, and temperature in the lower half of the header. The sky visual (sun + clouds) occupies the top 40% of the header. On scroll, the header compresses to 80px and the sun/clouds fade out gracefully using a CSS transition.

5. SCROLL BEHAVIOR
When the user scrolls down, the header should compress smoothly (transition: height 0.3s ease). The sun/cloud layer fades out (opacity: 0 at scroll > 80px). The city name and temperature remain visible in the compressed header. Do NOT make the entire header fixed — only the top bar with city name and temp sticks.

6. WEATHER CONDITION DETECTION LOGIC
Map these API condition keywords to visual themes:
- "Rain", "Drizzle", "Thunderstorm", "Monsoon", "Shower" → rainy theme (dark sky + rain animation + clouds at 0.85 opacity)
- "Cloud", "Overcast", "Mist", "Fog", "Haze" → cloudy theme (grey sky + clouds at 0.6 opacity)
- "Clear", "Sunny" → clear theme (bright sky + full sun)
- "Snow" → winter theme (light grey sky + snowflake CSS particles)
Build a getWeatherTheme(conditionString) function that returns the correct theme object.

The final result should feel like a real native weather app (reference: iOS Weather app or Google Weather). All animations must be smooth (60fps), using CSS transforms and opacity — never animate width/height or top/left directly.
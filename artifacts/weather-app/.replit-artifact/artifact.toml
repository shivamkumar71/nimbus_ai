kind = "web"
previewPath = "/"
title = "Weather App"
version = "1.0.0"
id = "artifacts/weather-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20578

[services.development]
run = "pnpm --filter @workspace/weather-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/weather-app", "run", "build" ]
publicDir = "artifacts/weather-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20578"
BASE_PATH = "/"
